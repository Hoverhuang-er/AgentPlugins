{
  "name": "mol3d-ui",
  "version": "0.1.0",
  "description": "3D Molecule Viewer UI with LangChain integration",
  "type": "module",
  "scripts": {
    "dev": "bun run server.ts",
    "build": "bun build src/index.ts --outdir dist --target browser",
    "build:wasm": "../build-wasm.sh",
    "start": "bun run server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@langchain/openai": "^0.0.19",
    "@langchain/core": "^0.1.30",
    "langchain": "^0.1.20",
    "surrealdb.js": "^0.11.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
